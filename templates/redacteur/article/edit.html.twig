{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Modifier l'article</h1><br>

    {{ include('redacteur/article/_form.html.twig', {'button_label': 'Modifier'}) }}<br>

    <div class="container">
        <div class="row justify-content-lg-start">
            <div class="col-md-auto">
                <a class="btn btn-secondary" href="{{ path('article_index') }}">Retour à la liste</a>
            </div>
            <div class="col-md-auto">
                {{ include('redacteur/article/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
